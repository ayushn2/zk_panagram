fn main(guess_hash: Field, answer_hash: pub Field, address : pub Field) {
    assert(address == address); // dummy operation to use the variable and avoid warnings
    assert(guess_hash == answer_hash);
}

// #[test]
// fn test_main() {
//     main(1, 2);

//     // Uncomment to make test fail
//     // main(1, 1);
// }
